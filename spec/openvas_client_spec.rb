require 'spec_helper'

describe OpenvasClient do
  it 'has a version number' do
    expect(OpenvasClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
